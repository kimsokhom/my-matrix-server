# Postmoogle Production Config
homeserver: "http://synapse.railway.internal:8008"
login: "@postmoogle:synapse-kim-sokhom-matrix-email-bridge.up.railway.app"
password: "RexformMail2024"

# This MUST be 'datasecret' (no underscore) to fix the "pickle key" crash
datasecret: "12345678901234567890123456789012"

database:
  dialect: "sqlite3"
  dsn: "/data/postmoogle.db"

domains:
  - "rexform-matrix.abrdns.com"

# Core Matrix Threading
threads: false

# DEFAULT ROOM SETTINGS (This solves your threading problem)
defaults:
  threadify: false # Do not use threads for email bodies
  nothreads: true # Do not use Matrix-native threads for chains
  # This owner string proves the file is being read correctly
  owner: "Production-Fix-Final"
