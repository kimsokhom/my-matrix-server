# Postmoogle Production Config
homeserver: "http://synapse.railway.internal:8008"
login: "@postmoogle:synapse-kim-sokhom-matrix-email-bridge.up.railway.app"
password: "RexformMail2024"

# This MUST be 'data_secret' (not 'secret') to fix the pickle error
data_secret: "12345678901234567890123456789012"

database:
  dialect: "sqlite3"
  dsn: "/data/postmoogle.db"

domains:
  - "rexform-matrix.abrdns.com"

# Core Bridge Settings
threads: false

# DEFAULT ROOM SETTINGS (This is where we solve your threading issue)
defaults:
  nothreads: true
  threadify: false
  # Prove the file is working
  owner: "Scratch-Fix-Verified"
