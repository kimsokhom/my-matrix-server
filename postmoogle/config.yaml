# --- SERVER ACCESS ---
homeserver: "http://synapse.railway.internal:8008"
login: "@postmoogle:synapse-kim-sokhom-matrix-email-bridge.up.railway.app"
password: "RexformMail2024"

# This MUST be nested like this for cfg.DataSecret (env.String("data.secret"))
data:
  secret: "12345678901234567890123456789012"

# --- DATABASE ---
db:
  dialect: "sqlite3"
  dsn: "/data/postmoogle.db"

# --- SMTP ---
domains:
  - "rexform-matrix.abrdns.com"
port: "25"

# --- BRIDGE CONTROL ---
# Global thread toggle
threads: false
loglevel: "INFO"

# Replace this with your actual Matrix ID to get Admin control
# admins:
#   - "@sokhom:synapse-kim-sokhom-matrix-email-bridge.up.railway.app"