# Postmoogle Production Configuration
homeserver: "http://synapse.railway.internal:8008"
login: "@postmoogle:synapse-kim-sokhom-matrix-email-bridge.up.railway.app"
password: "RexformMail2024"

database:
  dialect: "sqlite3"
  dsn: "/data/postmoogle.db"

domains:
  - "rexform-matrix.abrdns.com"

# Disable Matrix-Native Threads
threads: false

# DEFAULT ROOM SETTINGS
# This is the correct logic for new rooms
defaults:
  nothreads: true # Don't group emails into threads
  threadify: false # Don't put email bodies in threads
  noreplies: true # Don't "reply" to messages (removes the speech bubble icon)

  # PROOF OF CONFIG: Change this to a unique name
  # If !pm help shows this owner, you know the config file is working
  owner: "Production-Admin"
